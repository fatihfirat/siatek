/**
 * KULLANIM DISI — 19.09.2026
 *
 * Bu dosya, `src/hooks/useRealtimeData.ts` ile ayni isimde ikinci bir
 * useRealtimeData tanimliyordu ve HICBIR YERDEN import edilmiyordu
 * (grep ile dogrulandi). Icerigi 6 adet olu `fetch('/api/...')` cagrisiydi;
 * bu uygulamada sunucu olmadigi icin hepsi index.html donduruyordu.
 *
 * Icerik kaldirildi. Dosya, yanlislikla yeniden kullanilmasin diye
 * bos birakildi; guvenle silinebilir.
 */
export {};
